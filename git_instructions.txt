* (github) create new branch in github

* git pull

* git checkout new_branch

* Do the changes

view what are changed compared to branch
* git status

add new file to commit
* git add src/encapsulations/Main.java

* git rm src/encapsulations/WrongMain.java

* git commit -m "added main for encapsulations"

so far the commit is only on local machine, send to server
* git push

